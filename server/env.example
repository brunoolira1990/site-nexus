# Port for the server to run on
PORT=3001

# PostgreSQL Database connection details
DB_USER=postgres
DB_HOST=localhost
DB_DATABASE=nexusdb
DB_PASSWORD=your_password_here
DB_PORT=5432

# Secret for signing JWT tokens. Can be any long, random string.
JWT_SECRET=your_super_secret_string_for_jwt 